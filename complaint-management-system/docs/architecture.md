# Architecture
React frontend -> Express REST API -> PostgreSQL.

Authentication uses JWT. Authorization is role based.

Status flow: SUBMITTED -> UNDER_REVIEW -> ASSIGNED -> IN_PROGRESS -> RESOLVED -> CLOSED.
